<form method="post" action="{{ path('newsletter_delete', {'id': newsletter.id}) }}" onsubmit="return confirm('Es-tu sûr(e) de vouloir supprimer cette infolettre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newsletter.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
