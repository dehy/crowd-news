{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" {{ not newsletter.editable ? 'disabled' }}>{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
